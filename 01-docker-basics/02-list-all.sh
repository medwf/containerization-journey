#! /bin/bash
# list running container
docker ps # alias of docker container ls

# to list all conatiner
docker ps -a # alias docker container -a

docker ps -q # list ids of running conatiners
docker ps -aq # lsit ids of all continers

